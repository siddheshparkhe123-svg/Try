less(
    addDays(utcNow(), -30),
    parseDateTime(
        concat(
            substring(outputs('Compose'),6,4), '-',      // yyyy
            substring(outputs('Compose'),3,2), '-',      // MM
            substring(outputs('Compose'),0,2)            // dd
        )
    )
)
