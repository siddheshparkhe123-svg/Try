lessOrEquals(
    addDays(
        utcNow(),
        -30
    ),
    convertTimeZone(
        formatDateTime(
            substring(outputs('Compose'), 6, 4) & '-' &  substring(outputs('Compose'), 3, 2) & '-' & substring(outputs('Compose'), 0, 2),
            'yyyy-MM-dd'
        ),
        'UTC',
        'UTC'
    )
)
